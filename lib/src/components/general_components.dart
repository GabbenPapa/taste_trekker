import 'package:flutter/material.dart';

class VerticalGapDefault extends StatelessWidget {
  const VerticalGapDefault({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 8);
  }
}

class VerticalGapMedium extends StatelessWidget {
  const VerticalGapMedium({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: 16);
  }
}
